# public/

Drop static assets here. Two things are wired up but not yet present:

- `resume.pdf` — served at `/resume.pdf`; the terminal `resume` command and the ⌘K "Download resume" action both point here. Drop your PDF in this folder with that exact filename.
